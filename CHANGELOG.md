# Changelog

All notable changes to `quantara-toolkit` are documented here.

## [Unreleased]

### Added

- Placeholder repo structure (`cli/`, `runtime/`, `examples/`) reserving the shape of
  future Quantara developer tooling — see [README.md](README.md) for the roadmap.
- `cli`: Milestone 1 read-only CLI (`quantara init`, `quantara projects
  list`/`get`, `quantara logs`), built with Cobra against `quantara-core`'s REST
  API — see [cli/README.md](cli/README.md).
